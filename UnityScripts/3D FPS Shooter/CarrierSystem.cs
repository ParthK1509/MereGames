using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarrierSystem : MonoBehaviour
// , StandardControls.IGamePlayControlsActions
{
    [SerializeField]//so that private properties show up in the inspector
    private Transform RigSocket;
    [SerializeField]//so that private properties show up in the inspector
    private Animator RigAnimator;

    // private HandHeldScriptableObject
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
